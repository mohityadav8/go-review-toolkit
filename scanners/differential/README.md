# Differential parity checker

Compares a project's own generated or hand-duplicated code (for example,
mocks vs. the interfaces they're supposed to implement) and flags drift
between the two.

<!-- TODO: implement the parity-check algorithm and its CLI entrypoint -->
