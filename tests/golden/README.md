# Golden outputs

Expected-output snapshots for the fixtures in `tests/fixtures/`. Each
golden file records the findings a scanner/agent should produce for a
given fixture, so regressions in detection logic are caught automatically.

<!-- TODO: define the golden-file format and the comparison harness -->
